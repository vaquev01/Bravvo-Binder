[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "cd ../database && npx prisma generate && npx prisma db push --accept-data-loss && npx tsx seed.ts && cd ../api && node src/index.js"
