[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "cd ../Database && npx prisma generate && npx prisma db push --accept-data-loss && cd ../Backend && node src/index.js"
